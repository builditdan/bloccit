<h1>Topics</h1>
<br>

<div class="row">
  <div class="col-md-8">

   <!--
    The render method below calls "_topic_html.erb"
    iterating thru all the topics - the "_topic_html.erb"
    file is the default name looked for in /posts and
    it automatically iterates thru all the topics since
    it knows it is a collection. This is DRY code
    in action leveraging the concept of syntactic sugar
   -->
   <%= render @topics =%>

  </div>
  <div class="col-md-4">
    <% if user_is_authorized_to_delete_topics? %>
      <div class="col-md-4">
        <%= link_to "New Topic", new_topic_path, class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>

</div>
