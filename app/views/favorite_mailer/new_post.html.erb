<!DOCTYPE html>
 <html>
   <head>
     <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
   </head>
   <body>
     <h1>New post to "<%= @topic.name %>"</h1>
     <small>
       By: <%= @post.user.name %>
     </small>
     <br>
     <h5>
       Title:<%= @post.title %>
     </h5>
     <hr>
     <p>
       <%= @post.body %>
     </p>
     <p>

       <%= link_to "View Post on site", topic_post_url(@post.topic, @post, anchor: "post-#{@post.id}") %>
     </p>
   </body>
 </html>
