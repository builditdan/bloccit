New post to "<%= @topic.name %>"
<%= "=" * (@topic.name.length + 12) %>
By: <%= @post.user.name %>

<%= @post.title %>

---------------------------------------

<%= @post.body %>

Visit online at <%= topic_post_url(@post.topic, @post, anchor: "post-#{@post.id}") %>
